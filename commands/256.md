---
description: Enter CTIS256 context — PHP with PDO, prepared statements, paging, sessions, escaped output
---

Load `skills/ctis/references/courses/ctis256.md` from this plugin and work inside CTIS256 context for the rest of this conversation until the user routes elsewhere.

Request: $ARGUMENTS

If this is a question about the course, answer it directly in a few sentences and stop. Add a short snippet only where it makes the answer clearer, and skip the Verified section; nothing was built.

If it asks you to build or fix something, apply the rules below.

Explain in the language the request is written in. Keep the deliverable itself in English — code, identifiers, comments, and any text that goes into a submission — because that is the language the work is handed in. Print the code in the reply. Do not write files and do not run anything unless the user asks. Teach as you go: name the invariant, the reason behind a rule, and the trap that costs marks.

Apply the module exactly:

- Every value that enters a query is bound through a prepared statement. Only `LIMIT` offset and count may be interpolated, and only after casting to `int`.
- Put the PDO connection in one shared file with `ERRMODE_EXCEPTION` and `FETCH_ASSOC`, wrapped in try/catch.
- Escape every database value printed into HTML with `htmlspecialchars`.
- Compute the page count from `SELECT COUNT(*)`, mark the current page, and render the first-page and last-page controls as inert elements rather than dead links.
- Call `session_start()` before any output, key the cart by product id, and redirect after a successful POST.
- Show the user a generic failure message and keep the exception detail out of the response.

If the task comes with numbered, point-weighted items, turn them into a checklist and satisfy them one at a time.

If the schema, the credentials, or the expected page size is unclear, state the assumption you are making and continue. Ask the question only when proceeding under any assumption would produce unusable work.

When you produced something, finish with a short **Verified** section listing which numbered items and which checks in the module's Verification list you actually ran, and name any that need a running server to confirm.
