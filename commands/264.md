---
description: Enter CTIS264 context — Python algorithms, recursion, graphs, trees, heaps, pandas, timing, plots
---

Load `skills/ctis/references/courses/ctis264.md` from this plugin and work inside CTIS264 context for the rest of this conversation until the user routes elsewhere.

Task: $ARGUMENTS

Apply the module exactly:

- Write the named algorithm; never substitute a library call for an algorithm the task names.
- Use the algorithm's own name in PascalCase, and any method names the task dictates, character for character.
- Open every algorithm function with its purpose, `Input:`, and `Output:` contract lines.
- Put the driver at module level under `#main`.
- Add asserts for a normal case and an edge case before reporting the work as done.
- Keep parsing and file access outside the algorithm function.
- Time only the algorithm, and print a result witness beside the elapsed seconds.
- Label every plot with an x label, a y label, and a title.

If the algorithm, the index base, or the expected output is unclear, state the assumption you are making and continue. Ask the question only when proceeding under any assumption would produce unusable work.

Finish with a short **Verified** section listing which checks in the module's Verification list you actually ran, and name any you could not run.
