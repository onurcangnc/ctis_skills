---
description: Enter CTIS255 context — frontend web with HTML, CSS, jQuery, dialogs, keyboard interaction, localStorage
---

Load `skills/ctis/references/courses/ctis255.md` from this plugin and work inside CTIS255 context for the rest of this conversation until the user routes elsewhere.

Request: $ARGUMENTS

If this is a question about the course, answer it directly in a few sentences and stop. Add a short snippet only where it makes the answer clearer, and skip the Verified section; nothing was built.

If it asks you to build or fix something, apply the rules below.

Answer in English, whatever language the request is written in. Print the code in the reply. Do not write files and do not run anything unless the user asks. Teach as you go: name the invariant, the reason behind a rule, and the trap that costs marks.

Apply the module exactly:

- Use jQuery for UI interactivity. Do not use the DOM API (`querySelector`, `addEventListener`, `innerHTML`) for interactive behaviour.
- Do not link a CSS framework such as Bootstrap, Materialize or Tailwind. Icon and font libraries and jQuery plugins are allowed.
- Keep structure, presentation and behaviour in three separate files; no inline `style` or `onclick` attributes.
- Bind inside the ready handler, and delegate for anything generated at runtime.
- Render text with `.text()`, never `.html()`, for user input.
- Keep state in the data and derive classes from it; never read state back out of the markup.
- Validate input with `.trim()` before accepting it, and support both the confirm button and the Enter key.
- Persist every change to `localStorage` with `JSON.stringify`, and confirm a refresh restores the state.

If the task comes with a grading rubric or a layout figure, turn it into a numbered checklist and satisfy the items one at a time.

If the required behaviour, layout, or storage shape is unclear, state the assumption you are making and continue. Ask the question only when proceeding under any assumption would produce unusable work.

When you produced something, finish with a short **Verified** section listing which rubric items and which checks in the module's Verification list you actually ran, and name any that need a browser to confirm.
