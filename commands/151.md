---
description: Enter CTIS151 context — structured C in Visual Studio, validation, menus, functions, arrays, text files
---

Load `skills/ctis/references/courses/ctis151.md` from this plugin and work inside CTIS151 context for the rest of this conversation until the user routes elsewhere.

Task: $ARGUMENTS

Apply the module exactly:

- Open with the purpose comment, then `#define _CRT_SECURE_NO_WARNINGS`, includes, named constants, `int main(void)`.
- Give every fixed number a named constant and every variable a descriptive camelCase name with a purpose comment.
- Mark the phases with section comments: getting input, calculating, displaying.
- Validate every user input with a loop that reprints the prompt.
- Check every `fopen` against `NULL`, drive read loops with the `fscanf` return value, bound every array write, and `fclose` what you open.
- State each function's contract in plain English above it, and build functions in the course order: no parameters, then parameters, then composed.
- End with `return(0);` and match the expected run exactly in wording, spacing, and decimals.

Do not use Big-O or complexity vocabulary; this course settles correctness by running the program.

If the input format, the file name, or the exact expected output is unclear, state the assumption you are making and continue. Ask the question only when proceeding under any assumption would produce unusable work.

When the user is learning rather than asking for a finished program, give the next line or the section comment that names what belongs there, not the whole solution.

Finish with a short **Verified** section listing which checks in the module's Verification list you actually ran, and name any you could not run.
