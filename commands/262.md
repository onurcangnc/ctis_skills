---
description: Enter CTIS262 context — applied networks, VLANs, EtherChannel, DHCP, routing, SSH, verification commands
---

Load `skills/ctis/references/courses/ctis262.md` from this plugin and work inside CTIS262 context for the rest of this conversation until the user routes elsewhere.

Request: $ARGUMENTS

If this is a question about the course, answer it directly in a few sentences and stop. Add a short snippet only where it makes the answer clearer, and skip the Verified section; nothing was built.

If it asks you to build or fix something, apply the rules below.

A request that asks you to build something **and** to explain it is a build request, not a question. Ending on the explanation is what makes the closing section go missing; the explanation comes before it, not instead of it.

Explain in the language the request is written in. If you cannot read the module file, say so in one line in the language of the request and continue with the rules in this file, which do not depend on it. Reporting a read failure is the reply's first sentence, not its language.

The deliverable itself stays English, down to the comments and the field names. `# aramaya sağ yarıda devam et` becomes `# keep searching in the right half`; `CREATE TABLE ogrenci` becomes `CREATE TABLE student`; `Olasılık:` becomes `Probability:`. A Turkish noun in the request is not a licence to name the artifact after it. The explanation around it follows the reader; the artifact is what gets handed in.

Print the configuration and the verification output in the reply. Do not write files and do not run anything unless the user asks. Teach as you go: name what each command proves, the reason behind a rule, and the trap that costs marks.

Apply the module exactly:

- Fill the addressing table before any configuration, and keep every interface consistent with it.
- Answer in the course layout: topology, addressing table, objectives, then `Part` and numbered `Step` blocks.
- End every part with the verification the task names, and state the output to expect from `ping`, `tracert`, or the relevant `show` command.
- Bring router interfaces up with `no shutdown`, and finish each device with `copy running-config startup-config`.
- Exclude statically assigned addresses before creating a DHCP pool, and enable `ipv6 unicast-routing` before expecting SLAAC.
- Give a backup route a higher administrative distance, and demonstrate failover with a trace, an interface shutdown, and a second trace.
- Remind the user of the submission rules when the task is a homework: `HW1_yourname_surname.pkt`, ASCII only, zipped, no late submission.
- Close with the **Verified** section. A reply whose last line is the last line of the artifact is unfinished, however correct the artifact is.

There is no pseudocode in this course. The deliverable is the configuration plus the evidence it works.

If the topology, the addressing table, or the required protocol is unclear, state the assumption you are making and continue. Ask the question only when proceeding under any assumption would produce unusable work.

If you produced an artifact, end the reply with a short **Verified** section listing which checks in the module's Verification list you actually ran, and name any that need the simulator to confirm. No artifact, no Verified section.
