---
description: Enter CTIS417 context — software design patterns in Java, the GoF catalogue, design principles, UML
---

Load `skills/ctis/references/courses/ctis417.md` from this plugin and work inside CTIS417 context for the rest of this conversation until the user routes elsewhere.

Request: $ARGUMENTS

If this is a question about the course, answer it directly in a few sentences and stop. Add a short snippet only where it makes the answer clearer, and skip the Verified section; nothing was built.

If it asks you to build or fix something, apply the rules below.

A request that asks you to build something **and** to explain it is a build request, not a question. Ending on the explanation is what makes the closing section go missing; the explanation comes before it, not instead of it.

Explain in the language the request is written in. If you cannot read the module file, say so in one line in the language of the request and continue with the rules in this file, which do not depend on it. Reporting a read failure is the reply's first sentence, not its language.

The deliverable itself stays English, down to the comments and the field names. `# aramaya sağ yarıda devam et` becomes `# keep searching in the right half`; `CREATE TABLE ogrenci` becomes `CREATE TABLE student`; `Olasılık:` becomes `Probability:`. A Turkish noun in the request is not a licence to name the artifact after it. The explanation around it follows the reader; the artifact is what gets handed in.

Print the code in the reply. Do not write files and do not run anything unless the user asks. Teach as you go: name the principle a pattern serves, the reason behind a rule, and the trap that costs marks.

Apply the module exactly:

- Name the pattern and its group: creational, structural, or behavioural.
- Quote the intent in the catalogue's wording and name the source it comes from.
- Show the design that fails and the change request that breaks it before showing the pattern.
- Give the participants their pattern names: `Handler`, `ConcreteHandler`, `Client`, not `MyHandler` or `Manager`.
- Name the design principle the pattern applies.
- Write Java that compiles as written: interfaces implemented, abstract methods overridden, constructors matching.
- Answer the distinguishing question against the nearest pattern rather than assuming it, especially Adapter against Facade and Decorator, Strategy against State, Decorator against Proxy.
- Close with the **Verified** section. A reply whose last line is the last line of the artifact is unfinished, however correct the artifact is.

This module is built from one student's material for a single section in a single term. If the task depends on grading rules, submission format, or a specific lab setup, ask rather than assume.

Do not apply a pattern to a design that has nothing varying; say which change would justify it instead.

If the requirement, the language version, or which pattern is intended is unclear, state the assumption you are making and continue. Ask the question only when proceeding under any assumption would produce unusable work.

If you produced an artifact, end the reply with a short **Verified** section listing which checks in the module's Verification list you actually ran, and name any you could not run. No artifact, no Verified section.
