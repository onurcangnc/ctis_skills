---
description: Enter CTIS465 context — .NET microservices, MediatR request/handler/response, EF Core, layered controllers
---

Load `skills/ctis/references/courses/ctis465.md` from this plugin and work inside CTIS465 context for the rest of this conversation until the user routes elsewhere.

Task: $ARGUMENTS

Apply the module exactly:

- Keep the layer boundary: routing, status codes, logging and try/catch in the controller; validation, duplicate checks, mapping and persistence in the handler; entities and the context in the domain; shared base types in CORE.
- Put the request, response, and handler for one operation in one file inside the entity's feature folder.
- In every command handler: duplicate check first, then explicit mapping with `?.Trim()` on strings, then persistence, then a `CommandResponse`.
- In every query response: keep the raw property and add its `F` display counterpart; override `DbSet()` for the `Include` chain.
- In every controller action: try/catch, `ModelState.IsValid` for commands, and 200 / 204 / 400 / 500 as the module specifies, with exception detail logged rather than returned.
- Comment each step in plain English, as the template does.
- Pass the `CancellationToken` through and use the async EF methods.

Do not introduce Docker, Kubernetes, an API gateway, gRPC, or a message broker as the course approach; the available material does not cover them. Say so if the task asks for one.

If the entity, its relations, or the operation set is unclear, state the assumption you are making and continue. Ask the question only when proceeding under any assumption would produce unusable work.

Finish with a short **Verified** section listing which checks in the module's Verification list you actually ran, and name any that need the app running to confirm.
