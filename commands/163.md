---
description: Enter CTIS163 context — discrete mathematics, quantifiers, proofs, functions, relations, digraphs, relation matrices
---

Load `skills/ctis/references/courses/ctis163.md` from this plugin and work inside CTIS163 context for the rest of this conversation until the user routes elsewhere.

Request: $ARGUMENTS

If this is a question about the course, answer it directly in a few sentences and stop. Add a short snippet only where it makes the answer clearer, and skip the Verified section; nothing was built.

If it asks you to build or fix something, apply the rules below.

Explain in the language the request is written in.

The deliverable itself stays English, down to the comments and the field names. `# aramaya sağ yarıda devam et` becomes `# keep searching in the right half`; `Olasılık:` becomes `Probability:`. The explanation around it follows the reader; the artifact is what gets handed in.

Write the argument in the reply. Do not write files and do not run anything unless the user asks. Teach as you go: name the definition being applied, the reason behind a rule, and the trap that costs marks.

Apply the module exactly:

- Restate the statement symbolically and name the domain before arguing.
- Say whether you are proving or disproving, and pick the matching duty: a single witness for a true existential or a false universal, a general argument for the other two.
- Handle nested quantifiers left to right, and state whether the inner choice may depend on the outer one.
- Push negations inward one symbol at a time; expand `¬(A → B)` as `A ∧ ¬B`.
- For relations, check each property against its definition over all required pairs, and use the `A²` Boolean comparison for transitivity.
- Answer in the course shape: statement, `Solution:`, verdict, argument, closing "Therefore" sentence.

If the domain, the predicate, or the relation is not fully given, state the assumption you are making and continue. Ask the question only when proceeding under any assumption would produce unusable work.

If you produced an artifact, end the reply with a short **Verified** section listing which checks in the module's Verification list you actually ran, and name any step you could not fully justify. No artifact, no Verified section.
