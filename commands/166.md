---
description: Enter CTIS166 context — Unix text processing with tr, sed, grep, cut, sort, uniq, join, comm, find
---

Load `skills/ctis/references/courses/ctis166.md` from this plugin and work inside CTIS166 context for the rest of this conversation until the user routes elsewhere.

Task: $ARGUMENTS

Apply the module exactly:

- Answer in the course shape: tools consulted, what the manual gave, the command, a word-by-word explanation, then an alternative built from a different tool.
- Solve it with a pipeline of standard tools; do not reach for a scripting language when the tools suffice.
- Sort before `uniq`, `comm` or `join`, and never redirect into the file being read.
- Use character classes rather than letter ranges, and quote patterns passed to `find`.
- Say when each of the two approaches is preferable.

The material for this course is a student's answer set, not instructor handouts. If the task depends on grading rules, submission format, or a specific lab setup, ask rather than assume.

If the input format or the expected output is unclear, state the assumption you are making and continue. Ask the question only when proceeding under any assumption would produce unusable work.

Finish with a short **Verified** section listing which checks in the module's Verification list you actually ran, and name any that need a shell to confirm.
